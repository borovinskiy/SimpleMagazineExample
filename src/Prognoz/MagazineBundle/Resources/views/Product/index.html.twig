{% extends "PrognozMagazineBundle::view.html.twig" %}

{% block content -%}
    <h1>Product list</h1>

    {% for entity in entities %}               
          <div class="catalog-product-wrapper">  
              {% include 'PrognozMagazineBundle:Product:view.html.twig' with {'entity': entity} %} 
          </div>    
    {% endfor %}
    
    {#
    
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Price</th>
                <th>Photo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
                
                {% include 'PrognozMagazineBundle:Product:view.html.twig' with {'entity': entity} %}
                
                
                
            <tr>
                <td><a href="{{ path('api_product_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.price }}</td>
                <td>{{ entity.photo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('api_product_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('api_product_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
            
        {% endfor %}
        </tbody>
    </table>
#}
        <ul>
        <li>
            <a href="{{ path('api_product_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
